img.example.com {
    encode zstd gzip

    # Maximum authenticated batch: 10 files x 500 MB plus multipart overhead.
    request_body {
        max_size 5120MB
    }

    # Must match PORT in /etc/picnest/picnest.env.
    reverse_proxy 127.0.0.1:18765
}
